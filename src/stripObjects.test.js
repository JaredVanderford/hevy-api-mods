import { describe, it, expect, vi } from 'vitest';
import { stripExercise, stripRoutine, stripSet } from './stripObjects';

describe('stripObjects', () => {
    it('stripRoutine strips out all non specified values from a given routine', () => {})
    it('stripExercise strips out all non specified values from a given routine', () => {})
    it('stripSet strips out all non specified values from a given sets collection', () => {
        
    })
})